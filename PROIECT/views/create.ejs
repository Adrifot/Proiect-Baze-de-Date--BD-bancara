<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adaugare date</title>
</head>
<body>
    <h1>Adaugati date:</h1>
    <form action="/create/" method="GET">
        <label for="tableName">Adaugati in:</label>
        <select name="tableName" id="tableName" required>
            <option value="">Selectati Tabelul</option>
            <% data.forEach(row => { %>
                <option value="<%=row.TABLE_NAME%>"><%=row.TABLE_NAME%></option>
            <% });%>
        </select>
        <button>Adaugati date</button>
    </form>
    <a href="/">Inapoi</a>
    <script>
        const form = document.querySelector("form");
        const slct = document.querySelector("select");
        slct.addEventListener("change", () => {
            form.action += slct.value;
        });
    </script>
</body>
</html>